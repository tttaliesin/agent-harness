[project]
name = "agent-harness-web-api-fixture"
version = "0.1.0"
description = "Loopback HTTP fixture for real Python and browser checks"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest==9.1.1"]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
