--- skills/receiving-code-review/SKILL.md
+++ plugins/superpowers-execution/skills/receiving-code-review/SKILL.md
@@ -4,6 +4,9 @@
 ---
 
 # Code Review Reception
+
+Use the existing Core review gates and OpenSpec acceptance source.
+Apply feedback within the user's authorized scope, preserving settled decisions.
 
 ## Overview
 
@@ -41,10 +44,10 @@
 
 ```
 IF any item is unclear:
-  STOP - do not implement anything yet
+  Hold actions that depend on that item; continue independent understood work
   ASK for clarification on unclear items
 
-WHY: Items may be related. Partial understanding = wrong implementation.
+WHY: Resolve dependencies before acting, while preserving progress on independent authorized items.
 ```
 
 **Example:**
@@ -52,8 +55,8 @@
 your human partner: "Fix 1-6"
 You understand 1,2,3,6. Unclear on 4,5.
 
-❌ WRONG: Implement 1,2,3,6 now, ask about 4,5 later
-✅ RIGHT: "I understand items 1,2,3,6. Need clarification on 4 and 5 before proceeding."
+If 1,2,3,6 depend on 4 or 5, wait for the missing decision.
+✅ RIGHT: "I understand items 1,2,3,6. Need clarification on 4 and 5 before dependent changes; independent authorized work can continue."
 ```
 
 ## Source-Specific Handling
@@ -101,7 +104,7 @@
 
 ```
 FOR multi-item feedback:
-  1. Clarify anything unclear FIRST
+  1. Clarify unclear dependencies and continue independent understood items
   2. Then implement in this order:
      - Blocking issues (breaks, security)
      - Simple fixes (typos, imports)
@@ -170,7 +173,7 @@
 | Batch without testing | One at a time, test each |
 | Assuming reviewer is right | Check if breaks things |
 | Avoiding pushback | Technical correctness > comfort |
-| Partial implementation | Clarify all items first |
+| Missing dependencies | Clarify dependent items before implementing them |
 | Can't verify, proceed anyway | State limitation, ask for direction |
 
 ## Real Examples
@@ -202,4 +205,5 @@
 
 ## GitHub Thread Replies
 
-When replying to inline review comments on GitHub, reply in the comment thread (`gh api repos/{owner}/{repo}/pulls/{pr}/comments/{id}/replies`), not as a top-level PR comment.
+Send replies only when the user has authorized external communication.
+When authorized to reply to inline review comments on GitHub, reply in the comment thread (`gh api repos/{owner}/{repo}/pulls/{pr}/comments/{id}/replies`), not as a top-level PR comment.
